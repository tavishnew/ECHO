# The AI tutor calls the backend proxy (/api/ask), which holds the Groq key.
# In dev, Vite proxies /api -> backend (see vite.config.ts); no client key needed.
# For production, point the frontend at your deployed backend:
# VITE_API_URL=https://your-backend.onrender.com
